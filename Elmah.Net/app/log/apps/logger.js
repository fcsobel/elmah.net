(function () {

	// Declare app level module which depends on filters, and services
    angular.module('c3o.logger', [
	  'ngSanitize',
	  'ngCookies',
	  'ngRoute',
	  'ngAnimate',
	  'angularSpinner',
	  'c3o.core',
	]);

}());